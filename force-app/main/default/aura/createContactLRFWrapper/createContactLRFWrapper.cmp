<aura:component implements="force:lightningQuickActionWithoutHeader,force:hasRecordId" >
    <!--
        recordId をLWCに渡す。
        LWCのカスタムイベント'close'を受け取ったときのauraのハンドラをoncloseで指定している。
    -->
    <c:createContactLRF accountId="{!v.recordId}" onclose="{!c.close}"/>
    <!-- アクションの推奨の高さ: 340px -->
</aura:component>
